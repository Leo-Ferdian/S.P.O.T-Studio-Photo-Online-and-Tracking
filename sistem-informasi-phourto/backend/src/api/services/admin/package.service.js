const db = require('../../../config/database');
const apiError = require('../../../utils/apiError');

class AdminPackageService {
    // Implementasikan logika database di sini
    // (Untuk saat ini, biarkan kosong agar server bisa jalan)
}

module.exports = new AdminPackageService();